<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.veemed.veedoc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MessagesActivity"></activity>
        <activity android:name=".activities.DeferCallActivity" />
        <activity android:name=".activities.widgets.SearchableSpinner" />
        <activity android:name=".activities.widgets.SearchableSpinnerDialog" />
        <activity android:name=".activities.KartCallActivity" />
        <activity android:name=".activities.CompleteSignupActivity" /> <!-- <activity android:name=".test"></activity> -->
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.NavigationBarActivity"
            android:label="@string/title_activity_navigation_bar" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity
            android:name=".activities.SignUpActivity"
            android:theme="@style/Theme.MaterialComponents.Dialog" />
    </application>

</manifest>